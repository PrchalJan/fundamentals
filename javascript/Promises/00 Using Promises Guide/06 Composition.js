// https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Using_promises#composition

// Composition

// There are four composition tools for running asynchronous operations currently:
// Promise.all(), Promise.allSettled(), Promise.any(), and Promise.race().

// We can start opeprations at the same time and wait for them all to finish like this:

