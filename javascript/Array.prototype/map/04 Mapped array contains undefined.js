// 04 Mapped array contains undefined

// When undefined or nothing is returned, the resulting array contains undefined.
// If you want to delete the element instead, chain a filter() method,
// or use the flatMap() method and return an empty array to signify deletion.

const numbers = [1, 2, 3, 4];


// return all indexes lower than index 3
const filteredNumbers = null;


console.log(numbers);
console.log(filteredNumbers);
























































// const numbers = [1, 2, 3, 4];


// return all indexes lower than index 3
// const filteredNumbers = numbers.map((num, index)=>{
//     if(index < 3) {
//         return num;
//     }
// })


// console.log(numbers);
// console.log(filteredNumbers);
